import cn from "classname"
import styles from "@/styles/StatisticBox.module.scss"

export default function StatisticBox({
    ...props
}) {

    return (
        <div class={styles["statistic-box"]}>
            12
        </div>
    )
}